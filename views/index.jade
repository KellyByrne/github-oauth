extends layout

block content
  h1= title
  p Welcome to #{title}
   if user
      p Hello #{user.displayName} 
      img(src='#{user.avatar}' height="102" width="102")
      br
      a(href="/logout") Logout
    else
      p
        a(href="/auth/github") Login